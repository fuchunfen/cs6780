sudo apt-get install imagemagick gfortran freeglut3-dev libusb-1.0-0-dev cmake build-essential libhighgui-dev

To install:

Execute:
sudo ./install.sh

Edit:
RunBundler.sh
Specify the correct absolute path
